<template>
  <view class="vc" style="margin-top: 30rpx;" @tap="viewDetails" >
    <image src="{{item.cover}}" class="vc-image-article" />
    <text class="vc-text-article">{{item.title}}</text>
    <view class="vc-time">{{item.pubDate}}</view>
  </view>
</template>
<script>
  import wepy from 'wepy'
  export default class extends wepy.component {
    props = {
      item: Object,
      twoWay: true
    }
    methods = {
      viewDetails() {
        wepy.navigateTo({
          url: `techArticle?link=${this.item.link}`
        })
      }
    }
  }
</script>
<style lang="less">
  .vc{
    flex-wrap: wrap;
    .vc-image-article {
      width: 100vw;
      height: 270rpx;
    }
    .vc-text-article{
      width: 100vw;
      font-size: 35rpx;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 1;
      overflow: hidden;
      margin-top: 10rpx;
    }
    .vc-describe, .vc-time {
      text-align: left;
      width: 100%;
      position: relative;
      top: 10rpx;
    }
    .vc-time{
      top: 15rpx;
    }
  }
</style>
